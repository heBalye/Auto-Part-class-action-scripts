#get packages list
pip freeze

#Activate the virture environment
venv/Scripts/activate.bat

#build virture environment with system packages( second options)
python -m venv venv --system-site-packages

#build virture environment with list requirement. (best options when requirement list available.)
pip install -r requirement.txt

#delete a virture environment
rmdir venv /s

#list the packages installed only in this environment
pip list --local
